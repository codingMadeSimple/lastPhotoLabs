  //API route for fetching photos related to the clicked topic
  useEffect(() => {
    fetch('http://localhost:8001/api/topics/photos/:topic_id')
      .then(res => res.json())
      .then((data) => dispatch({ type: ACTIONS.GET_PHOTOS_BY_TOPIC, payload: data }));
  }, []);

    const clickTopic =()=>{
    dispatch({type: ACTIONS.GET_PHOTOS_BY_TOPIC, payload: {}})
  }
  
      case ACTIONS.GET_PHOTOS_BY_TOPIC:
      return {...state, photoData: action.payload};